package cluster

//InstallEtcdEX 拓展安装etcd
func (p *InstallPlan) InstallEtcdEX(sessionID int) error {
	//repo := currentClusterStatus.YUMStorageRepo
	//registry := currentClusterStatus.DockerRegistryURL

	//需要修改etcd容器启动参数中 etcd集群各个主机。。。

	return nil
}
